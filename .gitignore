# --- Python & Environment ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env
.env.*
!.env.example
.pytest_cache/
.coverage
htmlcov/

# --- IDE & Editors ---
.vscode/
.idea/
*.swp
*.swo

# --- Spark & Big Data Artifacts ---
metastore_db/
derby.log
spark-warehouse/
*.parquet
*.crc

# --- Project Data (CRITICAL) ---
data/
dags/
logs/
plugins/
venue/
# Output folder from transformations if any
outputs/

# --- Jupyter Notebooks ---
.ipynb_checkpoints/
# Optional: Ignore execution output in notebooks if you want clean commits
# *.ipynb_checkpoints

# --- Documentation Logs (Private) ---
# docs/DEV_JOURNAL.md # (User requested to keep this visible/tracked for now, un-comment if needed private)

# --- Root Markdown files (Keep README, ignore others if requested) ---
# *.md
# !README.md
# !docs/*.md
# Note: Usually we KEEP markdown files as they are documentation. 
# Attempting to ignore all root md files except README might hide important project info.
# I will keep .md files tracked unless strictly requested to Force Ignore.
# User said "ignore all md except docs", let's try to be specific.

# --- Temporary/Trash ---
tmp/
list_review.txt
